# Codex 网络观测版 v2rayN

该分支基于上游 v2rayN `7.25.0`，用于区分节点/本地网络可达性与 Codex 模型生成耗时。

## 第一阶段功能

- 在服务器列表中多选节点，右键执行“测试 Codex 连接”。v2rayN 为每个节点启动临时 SOCKS 入口，向 `https://chatgpt.com/backend-api/codex/responses` 发送固定 GET，并显示 HTTP 首响应时间（TTFB）中位数、P90、状态码和错误分类。
- 在“设置”中启用后台 Codex 连接审计，配置 5–1440 分钟间隔、1–365 天保留期和 1–10 个样本。
- 在“设置 → Codex 连接历史”查看最近 1000 条记录。
- 审计数据按保留期清理，并设 50000 行硬上限。

## 数据边界

审计只保存加盐哈希后的节点与网络指纹、网络接口类型、探测方式、样本数、TTFB、HTTP 状态和固定错误分类。它不保存节点地址、端口、密码、SSID、IP、网关、请求头、响应正文、Codex 凭据或对话内容。

## 指标解释

HTTP `401`、`403` 或 `405` 的响应可以用于测量网络路径已收到响应，但不代表模型服务健康。HTTP TTFB 也不是模型首 token：它不包含一次真实的已认证模型生成。

判断时建议看连续窗口：

| 现象 | 更可能的方向 |
|---|---|
| 同一网络下某节点失败率/P90 显著更差 | 节点或该节点到 ChatGPT 的路径 |
| 同一节点在手机热点明显改善 | 本地网络或运营商路径 |
| HTTP 探测稳定，但真实任务频繁断流 | WebSocket/长连接、Codex 客户端路径或服务侧 |
| 真实任务不重连，只是首段输出慢 | 排队、模型思考或任务复杂度 |

第一阶段不自动切节点。自动切换需要至少多个时间窗口，并加入失败率、P90、长连接断流和冷却时间，否则容易在短时抖动中反复切换。

## 第二阶段

手动的“真实 Codex 生成测试”应通过本机 `codex app-server` 和现有 ChatGPT 登录态执行固定、低推理、无工具、只读任务，记录首个可见文本增量。该指标会消耗 Codex 额度，也会混入服务排队与模型思考，因此不能作为高频后台探测。

被动关联 Codex 重试日志时，只允许按固定 target 与正则白名单汇总计数，不能复制日志正文；本机日志可能包含请求元数据和用户内容。

## 验证

- `ServiceLib.Tests`：100 项通过，包括 Codex 探测状态聚合、超时分类和分位数。
- `v2rayN.Desktop` Debug 完整编译通过。
- macOS arm64 Release 自包含发布通过。

本机已安装的 v2rayN 是定制的 `7.21.4`，上游没有对应 tag。替换前应备份配置并先并排运行该独立 Bundle ID 构建。
