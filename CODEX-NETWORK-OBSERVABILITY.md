# Codex 网络观测版 v2rayN

该分支基于上游 v2rayN `7.25.0`，用于区分节点/本地网络可达性与 Codex 模型生成耗时。

## 已实现功能

- 在服务器列表中多选节点，右键执行“测试 Codex 连接”。v2rayN 为每个节点启动临时 SOCKS 入口，向 `https://chatgpt.com/backend-api/codex/responses` 发送固定 GET，并显示 HTTP 首响应时间（TTFB）中位数、P90、状态码和错误分类。
- 服务器列表顶部提供“一键全量测试 Codex 连接（低干扰）”：先按 20 个节点分批、并发 2、每节点 1 个样本筛完整列表，再把当前节点与最快 5 个成功候选串行复测 5 个样本。按 Esc 可取消；全量测试期间后台审计不会并发探测。
- 在“设置”中启用后台 Codex 连接审计，配置 5–1440 分钟间隔、1–365 天保留期和 1–10 个样本。
- 在“设置 → Codex 连接历史”查看最近 1000 条记录、当前节点名称，以及同一时间窗内 Codex 客户端的重试、请求超时、断流、发送失败和 HTTP 回退计数。
- 历史页把当前节点与相同网络指纹下前后 30 分钟的手动候选测速比较，给出“立即重测候选”“先换接入网络/重测”“疑似长流稳定性”“短请求可达但不代表模型/服务正常”等建议。程序不会自动切换。
- 审计数据按保留期清理，并设 50000 行硬上限。

## 数据边界

审计保存加盐哈希后的节点与网络指纹、用于与本地配置联表显示名称的 `ProfileIndexId`、网络接口类型、探测方式、样本数、TTFB、HTTP 状态、固定错误分类，以及时间窗级别的 Codex 客户端事件计数。`ProfileIndexId` 不包含连接参数，但在对应配置尚存时可以关联回节点名称。审计不保存节点地址、端口、密码、SSID、IP、网关、请求头、响应正文、Codex 凭据或对话内容。

Codex 客户端事件来自 `~/.codex/logs_2.sqlite` 的只读聚合。读取器先核对表结构，再通过带时间范围的 SQL 仅匹配三个固定 target 和五类固定错误文本；日志正文始终留在 SQLite 内，不进入应用对象或审计库。数据库不存在、无权限或结构变化时只保存固定状态码并停止关联，不尝试宽泛扫描。

这些事件是整台机器上所有 Codex 任务的计数，并不对应某一个聊天。只有 v2rayN 从窗口开始前到采样结束持续观察到相同活动节点和网络指纹时，计数才标记为“上下文稳定”；应用启动后的首个窗口、节点切换或系统网络地址变化后的窗口标记为“整机未归属”，不会参与换节点判断。错误子类可能来自同一条重试日志，不能相加解释为独立故障次数。

## 指标解释

HTTP `401`、`403` 或 `405` 的响应可以用于测量网络路径已收到响应，但不代表模型服务健康。HTTP TTFB 也不是模型首 token：它不包含一次真实的已认证模型生成。

判断时建议看连续窗口：

| 现象 | 更可能的方向 |
|---|---|
| 同一网络下某节点失败率/P90 显著更差 | 节点或该节点到 ChatGPT 的路径 |
| 同一节点在手机热点明显改善 | 本地网络或运营商路径 |
| HTTP 探测稳定，但真实任务频繁断流 | WebSocket/长连接、Codex 客户端路径或服务侧 |
| 真实任务不重连，只是首段输出慢 | 网络证据不足以解释；再结合模型、推理强度、任务复杂度或服务排队排查 |

当前版本不自动切节点。建议遵循以下门槛：

- 当前节点样本必须在一小时内；候选必须来自相同的低置信度网络指纹、前后 30 分钟内、至少 5 个样本全部成功且 P90 小于 1000 ms。全量筛选阶段的单样本记录不参与换节点建议，只有优选复测记录参与。
- 只有候选 P90 不高于当前节点的 60% 时，才提示“立即重测候选”；重测仍有明显差距后才建议手动切换。
- 当前短请求良好但“上下文稳定”的 Codex 同窗发生重试、超时、断流或 HTTP 回退时，提示长流稳定性问题，并建议手动 A/B 复测；整机未归属窗口和一次窗口都不会触发自动切换。
- 当前节点很差且没有明显更好的候选时，优先建议重测或对比手机热点等接入网络。

## 尚未自动执行的真实生成测试

手动的“真实 Codex 生成测试”应通过本机 `codex app-server` 和现有 ChatGPT 登录态执行固定、低推理、无工具、只读任务，记录首个可见文本增量。该指标会消耗 Codex 额度，也会混入服务排队与模型思考，因此不能作为高频后台探测。

真实生成测试只适合作为手动诊断：它消耗 Codex 额度，而且首个可见文本同时受服务排队、模型与推理强度影响。当前版本通过被动事件关联先解决“能否换节点”的判断，不在后台发模型请求。

## 验证

- `ServiceLib.Tests`：115 项通过，包括 Codex 探测状态聚合、超时分类、分位数、全量测试候选筛选、日志白名单聚合、未知结构拒绝、旧审计表迁移、整机未归属/网络切换保护和换节点判断。
- `v2rayN.Desktop` Release 完整编译通过。
- macOS arm64 Release 自包含发布通过。

## macOS 打包与升级约束

- GUI 和 `AmazTool` 必须分别使用 `PublishSingleFile=true` 发布，只把两个单文件可执行程序及 GUI 原生库放入打包目录。`package-local-osx-app.sh` 会拒绝包含松散 `.dll`/`.deps.json`/`.runtimeconfig.json` 的输入，防止经裁剪的 `AmazTool` 框架文件覆盖 GUI 运行库。
- 升级前必须用隔离的本地配置目录真实启动候选 app，确认进程持续运行、无新崩溃报告，然后才可替换定制版。
- `/Applications/v2rayN.app` 是用户原先使用的 `7.21.4` 应急备用版，必须始终保留。升级只替换 `/Applications/v2rayN Codex Observability.app`；不移动、不改名、不清理原始备用版。
